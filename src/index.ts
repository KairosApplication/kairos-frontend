const idade: number = 16;
const nome: string = "Ana";
const aprovado: boolean = true;
const notas: number[] = [8, 9, 10];
const aluno: { nome: string; idade: number } = { nome, idade };

console.log(nome, idade, aprovado, notas, aluno);
